meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
      "identifier": "john@example.com",
      "password": "{{password}}"
    }
}

settings {
  encodeUrl: true
  timeout: 0
}
