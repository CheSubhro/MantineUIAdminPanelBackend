meta {
  name: Update Profile
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/auth/update
  body: json
  auth: inherit
}

body:json {
  {
      "name": "Tanvir Ahmed"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
